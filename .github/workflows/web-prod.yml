name: Web App - PROD
on:
    workflow_dispatch:
    workflow_call:
    push:
        branches:
          - main
          - chore/production-release
jobs:
    deploy-web-app:
        name: Deploy Web App
        uses: ./.github/workflows/web-app-deployer.yml
        with:
          FIREBASE_PROJECT: prod
        secrets: inherit

# Common packages
functions-framework==3.*
google-cloud-logging
psycopg2-binary==2.9.6
urllib3~=2.6.3
attrs~=23.1.0
certifi~=2025.8.3

# SQL Alchemy and Geo Alchemy
SQLAlchemy==2.0.23
geoalchemy2==0.14.7

# Google specific packages for this function
flask
google-cloud-storage
google-cloud-tasks

# The conversion itself. Reads the GTFS CSVs and writes Parquet; the only
# non-infrastructure dependency this function has.
duckdb~=1.5

# Runtime metrics
psutil

# Configuration
python-dotenv==1.2.2

# This file is a template for populating the terraform-init module variables
# To set variables:
# - Rename this file to vars.tfvars.
# - Replace variable values.
# - Execute your terraform script passing this file as a parameter, example: terraform plan -var-file=vars.tfvars

gcp_region={{REGION}}
project_id={{PROJECT_ID}}
environment={{ENVIRONMENT}}
deployer_service_account={{DEPLOYER_SERVICE_ACCOUNT}}
job_schedule={{JOB_SCHEDULE}}
datasets_bucket_name{{DATASETS_BUCKET_NAME}}